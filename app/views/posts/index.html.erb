<h1>Feeds<h1>
<table>
	<% @posts.each do |post| %>
    <tr>
      <td><%= link_to post.title , posts_path(post) %><td>
      <td><%= post.text %></td>
  	</tr>
  <% end %>
</table>
<%= link_to 'Back', posts_path %>
